# ============================================================
# 절대 Git에 올리면 안 되는 것들 (API 키, 계좌번호, 토큰)
# ============================================================
Key.txt
kis_config.json
kis_token_cache.json
kis_token_cache_TEST.json
kakao_token.json
user_config.json
alert_state.json
files/user_config.json
nh_config.json

# ============================================================
# 매일 생성되는 상태 파일 (원격에서 로그 확인엔 불필요, 계속 바뀌기만 함)
# ============================================================
position_state.json
nh_position_state.json
watchlist.json
strategy_params.json
last_weekly_run.txt
combined_screening_result.csv

# ⚠️ Log/, Order/, ScreeningLog/, ParamsLog/ 는 일부러 여기 넣지 않았습니다.
# 원격(GitHub)에서 로그를 확인하기 위해 이 폴더들은 반드시 추적(commit)돼야 합니다.
# (예전엔 "그냥 실행결과물이니 빼자"였는데, 이후 "로그를 GitHub으로 원격 확인하자"로
#  방향이 바뀌면서 이 폴더들은 오히려 꼭 올라가야 하는 대상이 됐습니다)

# ============================================================
# 파이썬 캐시
# ============================================================
__pycache__/
*.pyc
*.pyo

# ============================================================
# 로컬 도구 설정 (개인 환경 설정, 공유 불필요)
# ============================================================
.claude/settings.local.json

# ============================================================
# 기타
# ============================================================
*.bak
